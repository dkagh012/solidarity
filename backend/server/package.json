{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"react-scripts start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "server": "cd client/server && nodemon server.js",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "nodemon": "^3.0.1",
    "path": "^0.12.7"
  },
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

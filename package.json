{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start-front": "cd front && npm start",
    "start-back": "cd backend && cd server && nodemon server.js",
    "dev": "concurrently \"npm run start-front\" \"npm run start-back\""
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.4"
  }
}
